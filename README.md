I couldnt test it because intellij is taking 45 minutes+ to build so theres like a 30% chance in theory it works which if you think about it is 50-50 because .7^2 = .49 rounded to 50% if you run it twice
Features
- draw rectangle || probably works
- draw ngon || probably works
- draw bezeled || not implemented
- display video || not implemented
- window || very likely works
- loadlibrary || most likely works
