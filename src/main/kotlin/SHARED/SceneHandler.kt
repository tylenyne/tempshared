package crossxyed.SHARED

class SceneHandler() {

    /**fun sendGeometry(shapes: Array<Geometry>): Surface {
        while(shapes.isNotEmpty()) {
            when(shapes.last()::class.simpleName){
                "Rectangle" -> {
                    drawRectangle(shapes.last() as Rectangle, Scene.brush)
                }
                "Ngon" -> {
                    drawNgon(shapes.last() as Ngon, Scene.brush)
                }
            }
        }
        return null
    } **/

    fun overlay() {

    }
}